{
  "type": "object",
  "properties": {
    "startDate": {
      "type": "string",
      "description": "The requested date in YYYY-MM-DD format(e.g., 2025-06-10)."
    },
    "startTime": {
      "type": "string",
      "description": "The time the user wants to check availability for (e.g., '2:00 PM', '9:30 AM')."
    }
  },
  "required": [
    "startDate",
    "startTime"
  ]
}
